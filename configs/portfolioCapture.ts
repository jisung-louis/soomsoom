/**
 * Portfolio screenshot toggles.
 *
 * Keep both values false during normal development and before committing.
 * Turn on one flag at a time only when capturing portfolio images.
 */
export const PORTFOLIO_CAPTURE = {
  forceUpdateAlert: false,
  shopErrorScreen: false,
} as const;
